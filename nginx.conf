events {
    worker_connections 1024;
}

http {
    server {
        listen 80;
        server_name _;
        
        location / {
            return 200 "Nginx is working!";
            add_header Content-Type text/plain;
        }
    }
}